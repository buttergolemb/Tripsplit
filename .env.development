# Used automatically when running `vite` or `npm run dev` / `npm run dev:web`.
# Must include `/api` — routes are `/api/trips`, etc.; paths in api.ts are `/trips`, `/budget`, ...
VITE_API_BASE_URL=https://tripsplit-web-service.onrender.com/api
